[package]
name = "dndutils"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/toyo54/dndutils" #

[dependencies]
crossterm = { version = "0.29.0"}
rand = "0.10.0"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
